# Benchmark run weights

| Algorithm | Env | wandb run | Branch | Notes |
|-----------|-----|-----------|--------|-------|
| QAdapt (PPO swarm) | SuperSims All-XY | `genial-snow-649` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/l95x330v) | `supersims-qadapt` | 4 qubits, 50 iters, full virtualisation (compensation tensor rebuilt every step). Single shared `qubit_policy` (5-d action per qubit), MLP backbone. `log_std_bounds: [-10, 2]` (default — untrained std ≈ 7.4, clipped actions ≈ ±1 destroy baseline calibration on iter 1). Used as the inductive-bias baseline. |
| QAdapt + log_std cap (-1.5) + per-param split | SuperSims All-XY | `ujl5jot6` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/ujl5jot6) | `supersims-qadapt` | Killed at iter 3. log_std cap of -1.5 (std≈0.22) was too loose: 20-step RMS random walk ≈ 1× half-span, still wrecks calibration → iter 1 reward 134.34/400 ≈ 0.336 per-step (same as no-bias baseline). Did show ~2× faster gain rate (0.014/iter vs 0.006/iter baseline). Ckpts preserved at `checkpoints_supersims_run2_ujl5jot6/`. |
| QAdapt + log_std cap (-3, **non-functional**) + per-param split | SuperSims All-XY | `j9qojxjk` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/j9qojxjk) | `supersims-qadapt` | Killed at iter 1 (reward 136.77/400 ≈ 0.342). Investigating why the tighter cap didn't help revealed `log_std_bounds` had been **dead config** — `MLPFlatPolicyHead` never clamped log_std. So run2 and run3 were effectively running at std=1 throughout. Ckpts at `checkpoints_supersims_run3_j9qojxjk_capnotapplied/`. |
| QAdapt + log_std cap (-3, wired) + per-param split | SuperSims All-XY | `ycrma85q` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/ycrma85q) | `supersims-qadapt` | Killed at iter 1 (reward 107.45/400 = 0.27, **worse** than no-cap 0.33). Cause: default Kaiming init on the final policy layer gave untrained mean ≈ ±0.1 — under a tight std cap (0.05) sampling barely moves around mean, so each step pushes systematically in one direction → 2× half-span drift over 20 steps. Random sampling at std=1 had averaged this out before. Ckpts at `checkpoints_supersims_run4_ycrma85q/`. |
| QAdapt + cap (-3) + per-param + small-init policy head | SuperSims All-XY | `mjx6ojvr` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/mjx6ojvr) | `supersims-qadapt` | Killed at iter 3 to launch the narrow-range diagnostic run. iter 1: 230.96/400 = 0.577 (the inductive bias finally working — vs 0.27 in run4). iter 2: 0.587 (+0.010/iter). Slow climb from above-baseline starting point. Ckpts at `checkpoints_supersims_run5_mjx6ojvr/`. |
| Narrow-range diagnostic + full bias stack | SuperSims All-XY | `i26ydv9s` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/i26ydv9s) | `supersims-qadapt` | Killed at iter 2. iter 1: 0.619, iter 2: 0.635 (+0.016/iter — 1.6× faster than wide but still slow). Diagnosis: only narrowed *parameter sampling*, not the *action `delta_scales`* (those come from hardcoded constants in `normalisations.py`). So the agent's per-step action range was still calibrated for full-range tuning, dwarfing the narrower starting offsets. Ckpts at `checkpoints_supersims_run6_i26ydv9s/`. |
| Narrow-range + delta_scale 0.1 + full bias | SuperSims All-XY | `r85rfjs3` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/r85rfjs3) | `supersims-qadapt` | Killed at iter 1. iter 1 reward 359.54/400 = 0.899 — confirmed plumbing works. Per-policy diagnostic on iter 1 ckpt: all 5 policies emitting greedy actions ~0.001 (idle). Agent learned "do nothing" because narrow-config improvement budget (0.94 → 0.956 = 0.016) is dwarfed by std=0.05 noise cost (~0.3 per episode). Ckpts at `checkpoints_supersims_run7_r85rfjs3/`. |
| Medium-range (3×) + delta_scale 0.33 + full bias | SuperSims All-XY | `31z9h6xe` (https://wandb.ai/rl_agents_for_tuning/RLModel/runs/31z9h6xe) | `supersims-qadapt` | 3× narrowed param ranges with matched action scale. Reset reward avg 0.872 across 5 seeds (vs 0.94 narrow, 0.92 wide). Improvement budget ~0.13 — large enough that the agent must actively tune to maximise it, instead of converging to "do nothing". The diagnostic that tells us whether the algorithm learns *given a real signal*. | Same as run6 plus `delta_scale_factor: 0.1` — multiplies the per-step action scale by 0.1 so action `±1` now means a fine-tuning move (`±0.1 × half-span`), matching the 10× narrowed param ranges. Cumulative random walk under `std=0.05` cap drops to `~2% × half-span` over 20 steps, well within calibration tolerance. If this converges fast, the delta + compensation + small-action framing is sound. | Same as run5 but `parameter_config_filename: "parameter_config_narrow.json"` — sampling ranges 10× narrower around canonical midpoints. Reset reward avg 0.933 (5-seed), zero-action stays at 0.956 (ground-truth ceiling). Goal: verify the RL plumbing converges fast (~5-10 iters) on the easy distribution. If it doesn't, there's a real bug to chase before going back to full range. | Same as run4 but final policy layer uses orthogonal init with gain=0.01 (CleanRL/SB3 convention). Smoke-verified untrained mean ≈ 0.0005 (was ±0.1). Inductive bias finally complete: untrained policy emits actions ≈ 0 ± 0.05 → no systematic drift, no large random walk → iter-1 reward should land near the 0.92 zero-action baseline. | Same intent as run3 but with `log_std_bounds` actually plumbed into `MLPFlatPolicyHead._forward` (clamps log_std on every forward pass). Smoke-verified: log_std clamps to exactly -3, std ≈ 0.0498. Five shared per-param policies, `train_batch_size: 10240`. |
