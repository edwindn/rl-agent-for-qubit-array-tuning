To submit a job:
    sbatch scriptname.slurm

To check queued jobs (on htc):
    squeue -M htc -u $USER

To kill a job:
    scancel -M htc <job_id>

Running order:
    create .env file with WANDB_API_KEY=key
    run env_arc.slurm
    run arc.slurm